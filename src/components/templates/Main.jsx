import React from 'react';
import MapContainer from '../modules/map/MapContainer';

const Main = () => {
  return (
    <React.Fragment>
      <MapContainer></MapContainer>
    </React.Fragment>
  );
};

export default Main;
