import React from 'react';
import Grid from '../element/Grid/index';

const Main = props => {
  console.log(props);
  return (
    <div>
      <Grid>ss</Grid>
    </div>
  );
};

export default Main;
