import React from 'react';
import MapContainer from '../modules/map/MapContainer';

const Map = () => {
  return (
    <React.Fragment>
      <MapContainer></MapContainer>
    </React.Fragment>
  );
};

export default Map;
